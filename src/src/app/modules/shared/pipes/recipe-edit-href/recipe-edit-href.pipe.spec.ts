import {RecipeEditHrefPipe} from './recipe-edit-href.pipe';

describe('RecipeEditHrefPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeEditHrefPipe();
    expect(pipe).toBeTruthy();
  });
});
