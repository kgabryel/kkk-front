import {TagRecipesHrefPipe} from './tag-recipes-href.pipe';

describe('TagRecipesHrefPipe', () => {
  it('create an instance', () => {
    const pipe = new TagRecipesHrefPipe();
    expect(pipe).toBeTruthy();
  });
});
