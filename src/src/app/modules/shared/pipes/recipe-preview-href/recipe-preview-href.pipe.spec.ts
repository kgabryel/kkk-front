import {RecipePreviewHrefPipe} from './recipe-preview-href.pipe';

describe('RecipePreviewHrefPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipePreviewHrefPipe();
    expect(pipe).toBeTruthy();
  });
});
